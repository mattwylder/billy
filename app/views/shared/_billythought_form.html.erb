<%= form_for(@billythought) do |f| %>
    <div class="field">
        <%= f.text_area :content, placeholder: "compose new billythought..." %>
    </div>
    <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
