<% if logged_in? %>
    <div class="row">
        <aside class="col-md-4">
        <%= render 'shared/user_info' %>
        <%= render 'shared/billythought_form' %>
        </aside>
    </div>
<div class="col-md-8">
        <h3> Billythoughts </h3>
        <ol class="billythoughts">
            <%= render @billythoughts %>
        </ol>
</div>
<% else %>
    <%= link_to "login", login_path %> 
<h1>Billy's CLub is invite only ! !!</h1>
<p>teens only!</p>
<% end %>

<img alt="Rails" src="/assets/images/goat.gif" />

